# Daichi AgriFlow — Frontend

Next.js UI. Pages and NextAuth only; business logic calls the backend API.

## Setup

```bash
# From repo root (erp-daichi/)
npm install

cp .env.example .env
```

See `../backend/README.md` for full project setup and `npm run dev`.
